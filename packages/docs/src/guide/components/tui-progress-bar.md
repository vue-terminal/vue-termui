# `<TuiProgressBar>`

Creates progress bar.

```vue-html
<ProgressBar
  :value="50"
  color="green"
  bgColor="white"
  :width="12"
/>
```

## Aliases

`<TuiProgressBar>` can be also referenced as:

- `<ProgressBar>`
